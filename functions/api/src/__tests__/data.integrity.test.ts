/**
 * Data-layer integrity tests — Appwrite mode.
 * Full SQL/Turso tests removed. Keep as smoke placeholder.
 */
console.log('[test] data.integrity: skipped (Appwrite document store)');
process.exit(0);
